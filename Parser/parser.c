// ideia: gerar uma pilha para cada linha;
// iterar caractere por caractere;
// empilhar quando abre, desempilhar quando fecha, e não fazer nada quando não é *, _, /
// só desempilha se o espia() for igual, se não for então não esta bem formada.
// se chegar no final, então está bem formada
